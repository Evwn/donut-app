<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DonutApp" parent="Theme.MaterialComponents.DayNight">
        <!-- NoActionBar variant -->
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark</item>
        <item name="colorOnPrimary">@color/white</item>
        
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/colorAccent</item>
        <item name="colorSecondaryVariant">@color/colorAccentDark</item>
        <item name="colorOnSecondary">@color/white</item>
        
        <!-- Background color. -->
        <item name="android:windowBackground">@color/background</item>
        <item name="android:colorBackground">@color/background</item>
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/textPrimary</item>
        
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:navigationBarColor">@color/colorPrimaryDark</item>
        <item name="android:windowLightNavigationBar">false</item>
        
        <!-- Text colors -->
        <item name="android:textColorPrimary">@color/textPrimary</item>
        <item name="android:textColorSecondary">@color/textSecondary</item>
        <item name="colorOnBackground">@color/textPrimary</item>
        
        <!-- Elevation overlay -->
        <item name="elevationOverlayEnabled">true</item>
        <item name="elevationOverlayColor">@color/colorPrimaryDark</item>
        
        <!-- Toolbar style -->
        <item name="toolbarStyle">@style/Widget.App.Toolbar</item>
        
        <!-- Button style -->
        <item name="materialButtonStyle">@style/Widget.App.Button</item>
        <item name="materialButtonOutlinedStyle">@style/Widget.App.Button.Outlined</item>
        
        <!-- Card style -->
        <item name="cardViewStyle">@style/Widget.App.Card</item>
        
        <!-- Floating action button style -->
        <item name="floatingActionButtonStyle">@style/Widget.App.FloatingActionButton</item>
        
        <!-- Navigation view style -->
        <item name="navigationViewStyle">@style/Widget.App.NavigationView</item>
        
        <!-- Bottom navigation style -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
        
        <!-- Shape appearance -->
        <item name="shapeAppearanceSmallComponent">@style/ShapeAppearance.App.SmallComponent</item>
        <item name="shapeAppearanceMediumComponent">@style/ShapeAppearance.App.MediumComponent</item>
        <item name="shapeAppearanceLargeComponent">@style/ShapeAppearance.App.LargeComponent</item>
        
        <!-- Dialog style -->
        <item name="materialAlertDialogTheme">@style/ThemeOverlay.App.MaterialAlertDialog</item>
        <item name="alertDialogTheme">@style/ThemeOverlay.App.AlertDialog</item>
    </style>

    <!-- AppBar Overlay Theme -->
    <style name="Theme.DonutApp.AppBarOverlay" parent="ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:textColorSecondary">@color/white_87</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/white</item>
    </style>
    
    <!-- Popup Overlay Theme -->
    <style name="Theme.DonutApp.PopupOverlay" parent="ThemeOverlay.MaterialComponents.Light">
        <item name="android:colorBackground">@color/white</item>
        <item name="android:colorForeground">@color/black</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:windowBackground">@drawable/popup_background</item>
    </style>
    
    <!-- Toolbar Style -->
    <style name="Widget.App.Toolbar" parent="Widget.MaterialComponents.Toolbar.Primary">
        <item name="android:background">@color/colorPrimary</item>
        <item name="titleTextAppearance">@style/TextAppearance.App.Toolbar.Title</item>
        <item name="subtitleTextAppearance">@style/TextAppearance.App.Toolbar.Subtitle</item>
        <item name="android:elevation">4dp</item>
        <item name="titleTextColor">@color/white</item>
        <item name="subtitleTextColor">@color/white_87</item>
        <item name="navigationIconTint">@color/white</item>
        <item name="android:minHeight">?attr/actionBarSize</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="contentInsetStart">0dp</item>
        <item name="contentInsetEnd">0dp</item>
        <item name="contentInsetStartWithNavigation">0dp</item>
        
        <!-- Remove default title margin -->
        <item name="titleMarginStart">0dp</item>
        <item name="titleMarginEnd">0dp</item>
        <item name="titleMarginTop">0dp</item>
        <item name="titleMarginBottom">0dp</item>
        
        <!-- Custom title and subtitle styles -->
        <item name="titleTextAppearance">@style/TextAppearance.App.Toolbar.Title</item>
        <item name="subtitleTextAppearance">@style/TextAppearance.App.Toolbar.Subtitle</item>
    </style>
    
    <!-- Navigation View Style -->
    <style name="Widget.App.NavigationView" parent="Widget.MaterialComponents.NavigationView">
        <item name="itemIconTint">@color/nav_item_icon_tint</item>
        <item name="itemTextColor">@color/nav_item_text_color</item>
        <item name="itemBackground">@drawable/nav_item_background</item>
        <item name="itemRippleColor">@color/colorPrimaryLight</item>
        <item name="android:clipToPadding">false</item>
    </style>
    
    <!-- Bottom Navigation Style -->
    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemIconTint">@color/bottom_nav_item_icon_tint</item>
        <item name="itemTextColor">@color/bottom_nav_item_text_color</item>
        <item name="itemBackground">@color/white</item>
        <item name="itemRippleColor">@color/colorPrimaryLight</item>
        <item name="elevation">8dp</item>
    </style>
    
    <!-- Button Style -->
    <style name="Widget.App.Button" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/button_primary</item>
        <item name="android:textColor">@color/button_text</item>
        <item name="cornerRadius">24dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:paddingStart">24dp</item>
        <item name="android:paddingEnd">24dp</item>
        <item name="android:textAllCaps">false</item>
    </style>
    
    <!-- Card Style -->
    <style name="Widget.App.Card" parent="Widget.MaterialComponents.CardView">
        <item name="cardBackgroundColor">@color/card_background</item>
        <item name="cardElevation">4dp</item>
        <item name="cardCornerRadius">16dp</item>
        <item name="contentPadding">16dp</item>
        <item name="cardPreventCornerOverlap">false</item>
    </style>
    
    <!-- Floating Action Button Style -->
    <style name="Widget.App.FloatingActionButton" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="backgroundTint">@color/colorAccent</item>
        <item name="tint">@color/white</item>
        <item name="elevation">8dp</item>
        <item name="borderWidth">0dp</item>
    </style>
    <!-- NoActionBar theme variant -->
    <style name="Theme.DonutApp.NoActionBar" parent="Theme.DonutApp">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>